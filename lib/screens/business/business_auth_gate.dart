import 'package:flutter/material.dart';
import 'business_auth_wrapper.dart';

class BusinessAuthGate extends StatelessWidget {
  const BusinessAuthGate({super.key});

  @override
  Widget build(BuildContext context) {
    return const BusinessAuthWrapper();
  }
}
